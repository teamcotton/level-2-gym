#!/bin/sh

# Check if pnpm is available, if not use npm
if command -v pnpm >/dev/null 2>&1; then
  pnpm run format
  pnpm run lint
  pnpm run typecheck
  pnpm run test:unit
  pnpm run test:e2e
else
  echo "pnpm not found, skipping pre-push hooks in CI environment"
  exit 0
fi
