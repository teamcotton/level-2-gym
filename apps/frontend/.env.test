# Test Environment Variables
# These are used during E2E tests with Testcontainers

# Frontend
NEXT_PUBLIC_BASE_URL=http://localhost:4321

# Backend - DATABASE_URL will be set dynamically by global-setup.ts during tests.
# This placeholder value should never be used directly.
DATABASE_URL=__TO_BE_SET_BY_TEST_SETUP__
BACKEND_AI_CALLBACK_URL_DEV=http://localhost:3000
BACKEND_AI_CALLBACK_URL_PROD=http://localhost:3000

# Google API (use test key or mock)
GOOGLE_API_KEY=test-key-for-e2e
