'use client'

import { Typography } from '@mui/material'

export const MessageIntroComponent = () => {
  return (
    <Typography variant="body1" color="text.secondary">
      Start a conversation by typing a message below
    </Typography>
  )
}
